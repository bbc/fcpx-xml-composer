{
  "name": "@bbc/fcpx-xml-composer",
  "version": "1.1.1",
  "description": "Convert a json sequence into an FCPX XML.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish:public": "npm publish --access public",
    "publish:dry:run": "npm publish --dry-run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.6.1"
  }
}
